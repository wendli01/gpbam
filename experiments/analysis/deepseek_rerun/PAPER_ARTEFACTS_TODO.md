# Paper artefacts that need regenerating

Scope note: items 1, 2, 3, 4 and 5 are **done**; 6 is still blocked. This is the list, not the work. Reference draft is
`literature/paper/draft-20-08.tex` (2026-08-20).

Everything whose value depends on a judge score is affected, because the DeepSeek
judge seat changed underneath the published run (+4.04 mean, +7.27 on its own row).
Retrieval-recall figures, citation statistics and corpus-coverage tables are **not**
affected — they do not depend on judge scores.

## Where the artefacts actually come from

Not in `experiments/analysis/`. The paper's main and extended tables are built by
`src/notebook_eval/`, driven from notebooks (all last touched 2026-08-12, i.e. before
any of this was known):

| module | builds |
|---|---|
| `essay_evaluation.py` | main + extended essay-writing tables |
| `essay_correlation.py` | "Associations with essay quality" |
| `judge_correlation.py` | judge-by-model matrix, inter-judge agreement, ensemble vs individual |
| `article_recitation_evaluation.py` / `_plots.py` | article-recitation tables |
| `essay_plots.py` | essay-quality figures |

Driver notebooks: `essay_writing_norag.ipynb`, `essay_writing_lawrag.ipynb`,
`legal_knowledge.ipynb`, `judge_quality.ipynb`, `eassy_writing_rejudge.ipynb`.

`experiments/analysis/` covers the retrieval/corpus side and is mostly unaffected;
`paper_table.py`, `arm_summary.py`, `ladder_table.py`, `plot_cost_performance.py`,
`plot_aa_index_vs_essay.py`, `plot_recitation_vs_essay.py`, `judge_recall_slope.py`
and `oracle_experiment.py` all read judge scores and will move.

## To redo

### 1. Main essay-writing table
- Rebuild on the new three-seat panel (`gpt-oss-120b` / `Qwen3.6-35B` / `DeepSeek-V4-Flash-0731`).
- **Integrate the two article-recitation scores as columns**, so essay quality and
  recitation sit in one table.
- **Carry both DeepSeek rows**: `DeepSeek-V4-Flash` (published, retired checkpoint) and
  `DeepSeek-V4-Flash-0731`. Do not replace.
- Article recitation for DeepSeek also needs a re-run on 0731 — it is the same swapped
  model, so the published recitation number has the same provenance problem.

### 2. Extended tables
- Several will disappear with the new RAG setup (per draft `\checkthis`).
- **Keep and redo:** the cell-coloured judge-by-model matrix and the judge-bias table
  (`judge_correlation.py`). These get *more* interesting, not less — they now have a
  documented judge-version change to display, and the +7.27 self-preference effect.
- Inter-judge agreement — **DONE**, by `experiments/analysis/judge_agreement_zb/`
  (Zubaer) → `judge_agreement_zb.{tex,csv,pdf,png}`.
  Draft `Table 15` (long) and `Table 16` (compact) are the *old* panel — `gpt-5-nano`,
  26 models, ~2,100 essays — and are **deleted**, replaced by the one table the script
  writes on the reported panel (32 models, 2,592 essays). Its row set is read off
  `main_essay_table.csv`, the same parent `merged_results_table.py` reads for Table 5,
  and asserted to reproduce that file, so the two cannot drift apart.

### 3. "Associations with essay quality" / "what predicts essay quality"
- Redo on the new scores.
- **Missing one of the article-recitation columns** — add it.
- **Add the Artificial Analysis index** as a predictor
  (`plot_aa_index_vs_essay.py` already carries `AA_INDEX`).

### 4. Figures with essay quality on an axis — DONE
Regenerated by `scripts/rebuild_figures.py`, which drives both code paths off the
re-judged drop-in that `scripts/make_rejudged_result.py` writes:
- `cost_performance_norag` (in the draft)
- `aa_index_vs_essay_norag`
- `recitation_vs_essay_norag`
- `essay_quality_relationships_combined`, `quality_vs_answer_length`,
  `quality_vs_generation_cost`, `quality_vs_legal_reference_similarity`

### 5. AA index mapping — correctness fix — DONE
Split into two entries, matched on effort: published row → 0423 high (39), `-0731`
row → 0731 high (52). The same 0731-checkpoint assumption was also in
`plot_cost_performance.PRICES` and overstated the published row's cost by 1.7x;
fixed there too. Original note follows.

`plot_aa_index_vs_essay.py` maps `deepseek-ai/DeepSeek-V4-Flash` to the 0731
**max-effort** entry (52) on the docstring's claim that "FAU serves the 0731
checkpoint". Untrue for the published run, and `max` cannot be generated here at all
(600 s LiteLLM / ~1800 s proxy caps). Options:
- published row → older checkpoint's **high** entry (39);
- new `-0731` row → 0731 **high** entry, not max.
Either way the docstring needs correcting and the point moves ~13 index points.

### 6. Blocked on further runs
- **RAG arms re-judged** on the same three seats — needed before any cross-arm delta is
  trustworthy. The DeepSeek seat's RAG benefit (+4.74) is smaller than the checkpoint
  effect (+4.04), and `no_rag` was judged pre-swap while `rrf` was judged 2026-08-17.
- `judge_recall_slope.py` flat-credit decomposition depends entirely on those deltas.
- Oracle / `corpus_rag_panel` tables likewise.

## Provenance to state in the paper

The published `DeepSeek-V4-Flash` row is **not reproducible** — those weights are gone
from the serving. Whatever the final table looks like, the checkpoint and date should be
named for that row.
